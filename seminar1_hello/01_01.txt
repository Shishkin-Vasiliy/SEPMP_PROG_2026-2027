1.01: не подключен заголовочный файл stdio.h, выдаст ошибку что-то вроде "функция printf не обьявлена в этой зоне видимости"
1.02: т.к. в scanf не передан адрес a, эта переменная просто не меняется
1.03: вместо оператора == используется оператор присваивания
1.04: потому что в scanf передан символ \n и программа будет читать символы до тех пор, пока не встретится первый непробельный символ
1.05: это происходит потому что к if-у относится только первый printf, второй уже выполняется независимо от этого if-а 
1.06: после условия if не должно быть ';' иначе компилятор считает что выражение завершено
1.07: i не увеличивается и всегда равно 1
1.08: ';' после условия while - в данном случае бесконечный цикл который ничего не делает
1.09: sum не инициализировано, поэтому при выполнении sum += 1 (что эквивалентно sum = sum + 1, где компилятор не знает к какому sum прибавлять единицу) просто sum получит мусорное значени
1.10: выход за границы массива при обращении a[5]